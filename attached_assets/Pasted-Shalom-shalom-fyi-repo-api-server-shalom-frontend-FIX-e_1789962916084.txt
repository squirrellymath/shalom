Shalom (shalom.fyi repo -- api-server + shalom frontend)

**FIX: explicit handling of Bridget guest logins. Commit locally; do NOT push. Do not connect to production.**

Bridget now returns guests with `role: "guest"`, `is_guest: true` and an empty `email`. Before that change, guests arrived with an email beginning `__guest__` and `role: "free"`. Shalom's rule: a guest may not start or join a conversation.

1. **Reproduce first.** Run the SSO callback with a mocked verify response `{"user_id":"<id>","email":"","role":"guest","is_guest":true}`. Paste the current redirect, which is expected to be `/?auth_error=verify_failed`. Then run it with `{"user_id":"<id>","email":"__guest__x","role":"free"}` and paste the result, which is expected to be a successful login.
2. **Enumerate.**
   - List every place that reads the SSO email or role, with its path:line: the callback, the session user, `/member/status`, the access check, the invite-email match, and the message sender.
   - Report the counts of sessions, conversations, invites and messages whose email begins `__guest__`, from the dev database, and say that they are dev counts. Sessions are rows in `user_sessions` whose stored user email begins `__guest__`. Conversations and invites are `partner_email` or `invited_email` values. Messages are `sender` values. Change no rows.
3. **Callback and access check.**
   - Detect a guest when `is_guest === true`, when `role === "guest"`, or when the email begins `__guest__`, checked before the empty-email rejection.
   - For a guest, create no session and redirect to `/?auth_error=guest_not_supported`. A pending invite in the session stays pending and is not consumed, so the same link works after the person signs in with an email or Google account. Consume the SSO token exactly as for any other login.
   - At the access check, reject any existing session whose user email begins `__guest__` or whose role is `guest`. The conversation routes return 403, and `/member/status` returns `canAccess: false`.
   - Put the guest test in one shared function that both the callback and the access check use.
4. **Frontend.** Add `guest_not_supported` to the displayed auth errors with the text: "Shalom needs a signed-in Bridget account with an email. Guest sign-in can't start or join a conversation -- please sign in with email or Google."
5. **Tests.** Cover six cases:
   - (a) A guest with no pending invite gets the new redirect and no session.
   - (b) A guest with a pending invite gets the new redirect, the invite stays `pending`, and no partner is assigned.
   - (c) A normal login still succeeds.
   - (d) `role: "guest"` without `is_guest` is treated as a guest.
   - (e) A verify response with email `__guest__x`, `role: "free"` and no `is_guest` is treated as a guest.
   - (f) An existing session with a `__guest__` email is refused at the access check.
6. **Report:**
   - The enumeration list and the dev counts.
   - The files changed (path:line).
   - Both step-1 reproductions rerun after the fix, pasted beside the before output.
   - The test names and results, and the total test count.
   - The commit hash.

Before ending the turn, post the raw output of every action run in this turn.