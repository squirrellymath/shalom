[phases.setup]
nixPkgs = ["nodejs_22", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = [
  "PORT=8080 BASE_PATH=/ pnpm --filter @workspace/api-server run build",
  "PORT=8080 BASE_PATH=/ pnpm --filter @workspace/shalom run build",
]
